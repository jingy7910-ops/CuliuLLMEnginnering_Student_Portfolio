**10. 下周开发计划**

- **开发目标：** 完成 MVP 的第一条可运行链路：读取 SGF 棋谱，提取用户指定步数，并生成该步棋的基础复盘说明。

- **重点任务：**

1.  **SGF 解析模块：** 实现 `parse_sgf.py`，支持读取本地 `.sgf` 文件，输出总手数、对局双方信息、前 10 步坐标，以及用户指定步数的落子信息。

2.  **坐标语义化模块：** 将 SGF 坐标转换为棋盘方位描述，例如“右上角”、“左边三线”、“中腹附近”，为后续 LLM 生成解释提供更自然的输入。

3.  **Prompt 原型：** 设计第一版复盘 Prompt，让模型按照“当前局面位置 - 可能意图 - 初学者建议”的结构输出，不要求完整算清变化图，先保证表达稳定。

4.  **术语小知识库：** 整理 10 个高频围棋术语（如挂角、拆边、打入、征子、劫、眼、气、厚势、实地、定式），作为 Prompt 的参考词表。

5.  **基础测试样本：** 准备 3 个 SGF 棋谱样本，分别覆盖布局阶段、中盘战斗和官子阶段，记录解析结果与模型输出是否一致。

- **每日安排：**
完成开发环境检查，确认 Python、Ollama、SGF 示例文件均可正常使用。
完成 SGF 解析脚本，能够打印棋谱基本信息和指定步数坐标。
实现坐标到棋盘区域的转换逻辑，并用 10 个坐标点进行手工验证。
编写第一版 Prompt，接入 Qwen 模型，生成单步复盘说明。
整理术语词表和 3 个测试棋谱，记录模型输出中的坐标错误、术语错误和表达不清问题。
根据失败样本优化 Prompt，形成一份 300 字左右的阶段性开发总结。

- **下周交付物：**
`parse_sgf.py` 原型脚本。
3 个可用于测试的 SGF 样本。
第一版复盘 Prompt。
一份包含测试结果和失败案例的开发记录。

- **验收标准：** 输入一个 SGF 文件和指定步数后，系统能够正确返回该步坐标、棋盘区域描述，并生成一段不少于 100 字的初学者友好复盘说明。
